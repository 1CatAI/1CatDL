# 优先节点更新 · 2026-09-22

状态：用户批准后已于2026-09-22 02:01 +08:00上线。控制服务切换0.44秒，PID2689646→2764305；客户VM未重启。线上管理员/调度GET接口与公网页面、9项引用资源通过检查。完整过程见操作留痕/2026-09-22_015500-preferred-node-deploy.md。

回滚备份：`/var/lib/1cat-rental/backups/20260922-preferred-node`；原静态站点另保留在`/opt/1cat-rental/.public-before-placement-20260922`。本次没有实际搬迁控制器。原增量压缩包保持不变以保留经验证的SHA256；其中说明反映的是构建时待上线状态，本文件为上线后的最新记录。

## 交付内容

- 默认优先G2-002，客户新建只有主节点GPU不足、存储不足或离线时可以使用其他节点。
- 管理员设置新增“新实例优先节点”，支持调整优先节点与关闭限制，带并发更新检查和审计。
- GPU不足以实际占用槽位/完整四卡组判断；存储计入赠送容量、预留上限和物理安全余量。
- 网页提供自动推荐和不可选原因；后端事务再次校验，不能通过直接请求绕过。现有实例与幂等重试不重新分配节点。
- 为独立控制器准备local_node_id为空时的路由、资源统计与远程计费。正式迁移前必须遵循CONTROLLER_MIGRATION.md。

## 已通过

- Python完整回归：188项，251.761秒，全部通过。包含并发、鉴权、资源调度、远端计费、存储赠送和安装故障注入。
- 前端管理员/超时兼容测试：57项通过；TypeScript检查、本次变更定向lint、生产构建与静态资源检查通过。
- 本地浏览器：客户默认限制、管理员修改并保存、客户刷新同步、恢复默认和页面布局验证。
- 全项目lint另有16条历史通用组件/hooks问题，未在此功能中扩大修复。

## 本轮发布边界

1. 本次更新包仅是现有生产版本的增量更新，不是独立安装包。包含core/server/backend/remote_backend/placement及public和说明，不含数据库、密钥、客户密码或环境配置。
2. 保持当前RENTAL_LOCAL_NODE_ID默认G2-002，不启用独立控制器，不修改G2-003代理、不迁盘、不变更钱包/计费/镜像。
3. 上线前再读生产哈希；只有与下面基线匹配才可覆盖，否则先合并他人更新。确认无处理中操作，并记录当前运行VM与远程续租状态。
4. 先备份上述代码与public，SQLite作一致性备份，元数据/实例密钥作受限权限备份。正常代码回退必须保留最新业务数据库，不恢复旧账务。
5. 在同一受控切换中更换完整代码集和网页；placement.py不可遗漏。控制服务短暂重启会中断G2-002控制器自带的客户SSH转发，不应重启任何客户虚拟机。远程75秒租约不能超时。
6. 只更新本次文件；生产已有NeedDaemonReload=yes是本轮之前的状态，不擅自daemon-reload应用未知unit变动。
7. 复核服务健康、实例代次/运行状态、租约确认时间、公网SSH入口与新API。实例57总数据600GiB=付费200+赠送400必须保持不变。

### 01:40只读基线

| 文件 | SHA-256 |
| --- | --- |
| core.py | 85d3e9fe8c6832523f79efb56036d5b2a6dc1a5f0f4d2fa65bb691d89ebd48ec |
| server.py | 5e8bb71f3bc53e97e9efea67d60e76884eb14d096a1455146c8cdc45a1451654 |
| backend.py | 760255538026533a4c6e73dbb01d4dd249b6f21a08d645e1a53e12816f04b223 |
| remote_backend.py | 4603690723eb5f458330bed8c99e83c5423e9962e08c43fa5aba0ae0f08efa77 |

当时服务PID2689646；两机各一台四卡实例运行；无待处理动作。上述运行状态会变，部署前必须重查。

当前看板与G2-002同机：整机断电时看板也离线，本次仅完成独立部署准备，不宣称实现了控制面高可用。
